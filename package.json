{
  "name": "aws-bedrock-trix-editor",
  "type": "module",
  "version": "1.0.0",
  "description": "The AWS Bedrock Trix Editor is a form field component for the Filament PHP framework that enhances the Trix editor with Aws Bedrock features.",
  "main": "index.js",
  "bin": {
    "aws-bedrock-trix-editor": "bin/build.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:js": "node bin/build.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "dependencies": {
      "trix": "^1.3.1"
  }
}
